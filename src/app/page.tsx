export default function Home() {
  // Navigation is entirely handled globally by the AuthGuard component in layout.tsx.
  // This page will immediately be redirected to either /login or /dashboard.
  return null;
}
